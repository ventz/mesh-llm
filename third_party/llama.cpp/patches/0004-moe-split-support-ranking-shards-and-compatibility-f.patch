From 8127cfc0a1351f6d60bba464c7cb7f1b20f9c549 Mon Sep 17 00:00:00 2001
From: Mesh-LLM CI <ci@mesh-llm.local>
Date: Sun, 26 Apr 2026 08:25:17 +1000
Subject: [PATCH 4/6] moe-split: support ranking, shards, and compatibility
 fixes

---
 tools/moe-analyze/moe-analyze.cpp |  59 ++-
 tools/moe-split/moe-split.cpp     | 811 ++++++++++++++++++++++--------
 2 files changed, 661 insertions(+), 209 deletions(-)

diff --git a/tools/moe-analyze/moe-analyze.cpp b/tools/moe-analyze/moe-analyze.cpp
index d80b4bcb..4428e55c 100644
--- a/tools/moe-analyze/moe-analyze.cpp
+++ b/tools/moe-analyze/moe-analyze.cpp
@@ -51,6 +51,8 @@ struct callback_data {
     bool collect = true;
 };
 
+static std::string export_ranking_path;  // if non-empty, export per-expert mass to this file
+
 // ---- eval callback: intercept ffn_moe_probs ----
 
 static bool moe_callback(struct ggml_tensor * t, bool ask, void * user_data) {
@@ -234,7 +236,21 @@ int main(int argc, char ** argv) {
     params.prompt = ""; // will be set per-prompt
     params.n_predict = 32;
 
-    if (!common_params_parse(argc, argv, params, LLAMA_EXAMPLE_COMMON)) {
+    // Pre-scan argv for our custom args and strip them before common_params_parse
+    bool all_layers = false;
+    std::vector<const char *> filtered_argv;
+    for (int i = 0; i < argc; i++) {
+        if (strcmp(argv[i], "--export-ranking") == 0 && i + 1 < argc) {
+            export_ranking_path = argv[++i];
+        } else if (strcmp(argv[i], "--all-layers") == 0) {
+            all_layers = true;
+        } else {
+            filtered_argv.push_back(argv[i]);
+        }
+    }
+
+    int filtered_argc = (int)filtered_argv.size();
+    if (!common_params_parse(filtered_argc, const_cast<char **>(filtered_argv.data()), params, LLAMA_EXAMPLE_COMMON)) {
         return 1;
     }
 
@@ -290,6 +306,17 @@ int main(int argc, char ** argv) {
     cb_data.n_expert = n_expert;
     cb_data.n_expert_used = n_expert_used;
 
+    if (all_layers) {
+        cb_data.max_layers_to_log = 9999; // log all MoE layers
+        LOG_INF("Logging ALL MoE layers (--all-layers)\n");
+    }
+    if (!export_ranking_path.empty()) {
+        LOG_INF("Will export expert ranking to: %s\n", export_ranking_path.c_str());
+        if (!all_layers) {
+            LOG_INF("  NOTE: consider --all-layers for better ranking accuracy\n");
+        }
+    }
+
     // run prompts and collect routing data
     auto prompts = get_test_prompts();
     expert_stats stats;
@@ -405,6 +432,36 @@ int main(int argc, char ** argv) {
     std::sort(hot_experts.begin(), hot_experts.end(),
         [&](int a, int b) { return stats.total_mass[a] > stats.total_mass[b]; });
 
+    // Export ranking if requested
+    if (!export_ranking_path.empty()) {
+        double total = 0;
+        for (int e = 0; e < n_expert; e++) total += stats.total_mass[e];
+
+        std::ofstream fout(export_ranking_path);
+        if (!fout.is_open()) {
+            LOG_ERR("Failed to open export file: %s\n", export_ranking_path.c_str());
+        } else {
+            fout << "# MoE expert ranking by gate mass\n";
+            fout << "# Model: " << params.model.path << "\n";
+            fout << "# Experts: " << n_expert << " (top-" << n_expert_used << ")\n";
+            fout << "# Prompts: " << prompts.size() << " x " << n_gen << " tokens\n";
+            fout << "# Layers logged: " << (all_layers ? "all" : std::to_string(cb_data.max_layers_to_log)) << "\n";
+            fout << "# Total token-layer observations: " << stats.total_tokens << "\n";
+            fout << "#\n";
+            fout << "# Format: expert_id,gate_mass,mass_pct,selection_count\n";
+            fout << "# Sorted by gate_mass descending (hottest first)\n";
+            for (int i = 0; i < n_expert; i++) {
+                int e = hot_experts[i];
+                fout << e << "," << stats.total_mass[e] << ","
+                     << (100.0 * stats.total_mass[e] / total) << ","
+                     << stats.selection_count[e] << "\n";
+            }
+            fout.close();
+            LOG_INF("\nExported expert ranking to: %s\n", export_ranking_path.c_str());
+            LOG_INF("Use with moe-split: --group-map <file generated from this ranking>\n\n");
+        }
+    }
+
     LOG_INF("\n=== Group Masking Analysis (best-group capture ratio) ===\n");
     LOG_INF("For each group count, what fraction of the unrestricted top-%d mass\n", n_expert_used);
     LOG_INF("is captured by the best single group?\n\n");
diff --git a/tools/moe-split/moe-split.cpp b/tools/moe-split/moe-split.cpp
index f0ffc98b..9fabef2f 100644
--- a/tools/moe-split/moe-split.cpp
+++ b/tools/moe-split/moe-split.cpp
@@ -7,20 +7,19 @@
 //
 // Expert selection modes:
 //   1. Contiguous: --groups N splits experts into N equal contiguous groups
-//   2. Custom:     --expert-list 4,72,80,... selects specific experts by ID
+//   2. Balanced:   --groups N --balanced uses router gate norms to distribute
+//                  hot/cold experts evenly across groups (snake draft)
+//   3. Custom:     --expert-list 4,72,80,... selects specific experts by ID
+//   4. Group map:  --group-map FILE reads per-group expert IDs from a file
 //
 // Router gate rows are gathered to match the selected experts, so
 // expert i in the output GGUF has the correct routing weights.
 //
 // Usage:
-//   llama-moe-split --model input.gguf --groups 4 --output-prefix node
-//   → produces node-0.gguf, node-1.gguf, node-2.gguf, node-3.gguf
-//
-//   llama-moe-split --model input.gguf --groups 4 --group-id 2 --output node2.gguf
-//   → produces only group 2
-//
-//   llama-moe-split --model input.gguf --expert-list 64,65,72,80 -o custom.gguf
-//   → produces a GGUF with only those 4 experts (renumbered 0-3)
+//   llama-moe-split -m model.gguf -g 4 --output-prefix node
+//   llama-moe-split -m model.gguf -g 4 --balanced --output-prefix node
+//   llama-moe-split -m model.gguf --expert-list 64,65,72,80 -o custom.gguf
+//   llama-moe-split -m model.gguf --group-map groups.txt -g 4 --output-prefix node
 
 #include "ggml.h"
 #include "gguf.h"
@@ -28,12 +27,14 @@
 
 #include <algorithm>
 #include <cinttypes>
+#include <climits>
 #include <cmath>
 #include <cstdio>
 #include <cstdlib>
 #include <cstring>
 #include <fstream>
-#include <regex>
+#include <numeric>
+#include <set>
 #include <sstream>
 #include <string>
 #include <vector>
@@ -42,10 +43,14 @@ struct moe_split_params {
     std::string input;
     std::string output;           // single output (with --group-id or --expert-list)
     std::string output_prefix;    // prefix for multi-output (with --groups)
+    std::string group_map_file;   // file with per-group expert IDs
+    std::string ranking_file;     // CSV from moe-analyze --export-ranking
     int n_groups   = 0;
     int group_id   = -1;          // -1 = produce all groups
     bool dry_run   = false;
-    std::vector<int> expert_list; // custom expert selection (empty = use contiguous groups)
+    bool balanced  = false;       // use router gate norm balancing
+    int  replicate = 0;           // replicate top-N experts to every group
+    std::vector<int> expert_list; // custom expert selection (empty = use groups)
 };
 
 static void print_usage(const char * prog) {
@@ -58,12 +63,23 @@ static void print_usage(const char * prog) {
     printf("  -m, --model FILE          input GGUF file\n");
     printf("  -g, --groups N            number of expert groups to split into\n");
     printf("  --group-id N              produce only this group (0-indexed)\n");
+    printf("  --balanced                distribute experts by router gate norm (snake draft)\n");
+    printf("  --ranking-file FILE       CSV from moe-analyze --export-ranking (snake draft by\n");
+    printf("                            actual gate mass; overrides --balanced norm heuristic)\n");
+    printf("  --replicate N             replicate top-N hot experts to every group\n");
     printf("  --expert-list IDs         comma-separated expert IDs (e.g. 64,65,72,80)\n");
+    printf("  --group-map FILE          file with per-group expert IDs (one group per line,\n");
+    printf("                            comma-separated; overrides --balanced/contiguous)\n");
     printf("  -o, --output FILE         output file (requires --group-id or --expert-list)\n");
     printf("  --output-prefix PREFIX    output prefix (produces PREFIX-N.gguf)\n");
     printf("  --dry-run                 show plan without writing\n");
     printf("  -h, --help                show this help\n");
     printf("\n");
+    printf("Group map file format (one line per group):\n");
+    printf("  64,65,66,67,0,1,2,3\n");
+    printf("  32,33,34,35,96,97,98,99\n");
+    printf("  ...\n");
+    printf("\n");
 }
 
 static std::vector<int> parse_int_list(const std::string & s) {
@@ -71,7 +87,54 @@ static std::vector<int> parse_int_list(const std::string & s) {
     std::stringstream ss(s);
     std::string token;
     while (std::getline(ss, token, ',')) {
-        result.push_back(std::atoi(token.c_str()));
+        // trim whitespace
+        size_t start = token.find_first_not_of(" \t");
+        if (start == std::string::npos) continue;
+        result.push_back(std::atoi(token.c_str() + start));
+    }
+    return result;
+}
+
+// Read group map file: one group per line, comma-separated expert IDs
+// Lines starting with # are comments, empty lines are skipped
+static std::vector<std::vector<int>> read_group_map(const std::string & path) {
+    std::vector<std::vector<int>> groups;
+    std::ifstream f(path);
+    if (!f.is_open()) {
+        fprintf(stderr, "error: cannot open group map file: %s\n", path.c_str());
+        exit(1);
+    }
+    std::string line;
+    while (std::getline(f, line)) {
+        // skip comments and empty lines
+        size_t start = line.find_first_not_of(" \t");
+        if (start == std::string::npos) continue;
+        if (line[start] == '#') continue;
+        auto ids = parse_int_list(line);
+        if (!ids.empty()) {
+            groups.push_back(std::move(ids));
+        }
+    }
+    return groups;
+}
+
+// Read ranking CSV from moe-analyze: expert_id,gate_mass,mass_pct,selection_count
+// Returns vector of {expert_id, gate_mass} sorted by mass descending
+static std::vector<std::pair<int,double>> read_ranking_file(const std::string & path) {
+    std::vector<std::pair<int,double>> result;
+    std::ifstream f(path);
+    if (!f.is_open()) {
+        fprintf(stderr, "error: cannot open ranking file: %s\n", path.c_str());
+        exit(1);
+    }
+    std::string line;
+    while (std::getline(f, line)) {
+        if (line.empty() || line[0] == '#') continue;
+        int id;
+        double mass;
+        if (sscanf(line.c_str(), "%d,%lf", &id, &mass) >= 2) {
+            result.push_back({id, mass});
+        }
     }
     return result;
 }
@@ -88,8 +151,16 @@ static void parse_args(int argc, const char ** argv, moe_split_params & params)
             params.n_groups = std::atoi(argv[++i]);
         } else if (arg == "--group-id") {
             params.group_id = std::atoi(argv[++i]);
+        } else if (arg == "--balanced") {
+            params.balanced = true;
+        } else if (arg == "--ranking-file") {
+            params.ranking_file = argv[++i];
+        } else if (arg == "--replicate") {
+            params.replicate = std::atoi(argv[++i]);
         } else if (arg == "--expert-list") {
             params.expert_list = parse_int_list(argv[++i]);
+        } else if (arg == "--group-map") {
+            params.group_map_file = argv[++i];
         } else if (arg == "-o" || arg == "--output") {
             params.output = argv[++i];
         } else if (arg == "--output-prefix") {
@@ -109,19 +180,22 @@ static void parse_args(int argc, const char ** argv, moe_split_params & params)
 
     // Validate argument combinations
     if (!params.expert_list.empty()) {
-        // Custom expert list mode
         if (params.output.empty()) {
             fprintf(stderr, "error: --output is required with --expert-list\n");
             exit(1);
         }
-        if (params.n_groups > 0) {
-            fprintf(stderr, "error: --groups and --expert-list are mutually exclusive\n");
+        if (params.n_groups > 0 || !params.group_map_file.empty()) {
+            fprintf(stderr, "error: --expert-list is mutually exclusive with --groups/--group-map\n");
+            exit(1);
+        }
+    } else if (!params.group_map_file.empty()) {
+        if (params.output_prefix.empty() && params.output.empty()) {
+            fprintf(stderr, "error: --output-prefix (or --output with --group-id) required with --group-map\n");
             exit(1);
         }
     } else {
-        // Contiguous group mode
         if (params.n_groups <= 0) {
-            fprintf(stderr, "error: --groups or --expert-list is required\n");
+            fprintf(stderr, "error: --groups, --expert-list, or --group-map is required\n");
             exit(1);
         }
         if (params.group_id >= 0 && params.output.empty()) {
@@ -135,16 +209,22 @@ static void parse_args(int argc, const char ** argv, moe_split_params & params)
     }
 }
 
-// Check if tensor name matches an expert-packed tensor (ffn_gate_exps, ffn_up_exps, ffn_down_exps)
+// Check if tensor name matches an expert-packed tensor
 static bool is_expert_tensor(const char * name) {
     return strstr(name, "ffn_gate_exps") != nullptr
         || strstr(name, "ffn_up_exps")   != nullptr
-        || strstr(name, "ffn_down_exps") != nullptr;
+        || strstr(name, "ffn_down_exps") != nullptr
+        || strstr(name, "exp_probs_b")   != nullptr;
 }
 
-// Check if tensor is the MoE router gate (ffn_gate_inp)
+// Check if tensor is a shared-expert tensor (trunk, not sliced)
+static bool is_shared_expert(const char * name) {
+    return strstr(name, "_shexp") != nullptr;
+}
+
+// Check if tensor is the MoE router gate (but not the shared-expert gate)
 static bool is_router_gate(const char * name) {
-    return strstr(name, "ffn_gate_inp") != nullptr;
+    return strstr(name, "ffn_gate_inp") != nullptr && !is_shared_expert(name);
 }
 
 static void zeros(std::ofstream & f, size_t n) {
@@ -154,18 +234,211 @@ static void zeros(std::ofstream & f, size_t n) {
     }
 }
 
+struct input_shard {
+    std::string path;
+    struct gguf_context * ctx_gguf;
+    struct ggml_context * ctx_meta;
+};
+
+static std::vector<input_shard> load_input_shards(const std::string & input_path) {
+    std::vector<input_shard> shards;
+
+    auto load_one = [&shards](const std::string & path) {
+        struct ggml_context * ctx_meta = nullptr;
+        struct gguf_init_params init_params = {
+            /*.no_alloc = */ true,
+            /*.ctx      = */ &ctx_meta,
+        };
+
+        auto * ctx_gguf = gguf_init_from_file(path.c_str(), init_params);
+        if (!ctx_gguf) {
+            fprintf(stderr, "error: failed to load %s\n", path.c_str());
+            exit(1);
+        }
+
+        shards.push_back({ path, ctx_gguf, ctx_meta });
+    };
+
+    load_one(input_path);
+
+    int split_count_idx = gguf_find_key(shards[0].ctx_gguf, "split.count");
+    if (split_count_idx < 0) {
+        return shards;
+    }
+
+    int n_split = gguf_get_val_u16(shards[0].ctx_gguf, split_count_idx);
+    if (n_split <= 1) {
+        return shards;
+    }
+
+    std::vector<char> split_prefix(PATH_MAX, 0);
+    if (!llama_split_prefix(split_prefix.data(), split_prefix.size(), input_path.c_str(), 0, n_split)) {
+        fprintf(stderr, "error: unexpected split input file name: %s\n", input_path.c_str());
+        exit(1);
+    }
+
+    for (int i_split = 1; i_split < n_split; i_split++) {
+        std::vector<char> split_path(PATH_MAX, 0);
+        int ret = llama_split_path(
+            split_path.data(),
+            split_path.size(),
+            split_prefix.data(),
+            i_split,
+            n_split);
+        if (ret <= 0) {
+            fprintf(stderr, "error: failed to derive split path for shard %d/%d\n", i_split + 1, n_split);
+            exit(1);
+        }
+        load_one(split_path.data());
+    }
+
+    return shards;
+}
+
+static const input_shard * find_input_shard_for_tensor(
+        const std::vector<input_shard> & input_shards,
+        const char * name,
+        int * shard_index_out,
+        int * tensor_index_out) {
+    for (int shard_index = 0; shard_index < (int) input_shards.size(); shard_index++) {
+        int tensor_index = gguf_find_tensor(input_shards[shard_index].ctx_gguf, name);
+        if (tensor_index >= 0) {
+            if (shard_index_out) {
+                *shard_index_out = shard_index;
+            }
+            if (tensor_index_out) {
+                *tensor_index_out = tensor_index;
+            }
+            return &input_shards[shard_index];
+        }
+    }
+
+    return nullptr;
+}
+
+// Compute per-expert average L2 norm across all router gate layers.
+// Router gates are [n_embd, n_expert] F32 tensors — each row (column in logical
+// layout) is an expert's routing weight vector. We compute ||w_e||_2 for each
+// expert e, averaged across layers.
+static std::vector<double> compute_gate_norms(
+        const std::vector<input_shard> & input_shards,
+        int n_expert) {
+    std::vector<double> norm_sum(n_expert, 0.0);
+    int n_layers = 0;
+
+    for (const auto & input_shard : input_shards) {
+        std::ifstream f_in(input_shard.path, std::ios::binary);
+        if (!f_in.is_open()) {
+            fprintf(stderr, "error: cannot open %s\n", input_shard.path.c_str());
+            exit(1);
+        }
+
+        int n_tensors = gguf_get_n_tensors(input_shard.ctx_gguf);
+        for (int i = 0; i < n_tensors; i++) {
+            const char * name = gguf_get_tensor_name(input_shard.ctx_gguf, i);
+            if (!is_router_gate(name)) continue;
+
+            struct ggml_tensor * t = ggml_get_tensor(input_shard.ctx_meta, name);
+
+            // Router gate shape: [n_embd, n_expert] — ne[0]=n_embd, ne[1]=n_expert
+            int n_embd_gate = (int) t->ne[0];
+            int n_exp_gate  = (int) t->ne[1];
+
+            if (n_exp_gate != n_expert) {
+                fprintf(stderr, "warning: gate %s has %d experts, expected %d. Skipping.\n",
+                    name, n_exp_gate, n_expert);
+                continue;
+            }
+
+            // Read gate data — must be F32 for norm computation
+            if (t->type != GGML_TYPE_F32) {
+                fprintf(stderr, "warning: gate %s is type %d (not F32). Skipping norm analysis.\n",
+                    name, t->type);
+                continue;
+            }
+
+            size_t offset =
+                gguf_get_data_offset(input_shard.ctx_gguf) +
+                gguf_get_tensor_offset(input_shard.ctx_gguf, i);
+            size_t n_bytes = (size_t) n_embd_gate * n_exp_gate * sizeof(float);
+            std::vector<float> data(n_embd_gate * n_exp_gate);
+            f_in.seekg(offset);
+            f_in.read((char *) data.data(), n_bytes);
+
+            // Compute L2 norm per expert
+            // Memory layout: expert e's weights are at data[e * n_embd .. (e+1) * n_embd)
+            // (row-major, each "row" in memory is one expert's embedding-dim vector)
+            for (int e = 0; e < n_expert; e++) {
+                double sum_sq = 0;
+                for (int d = 0; d < n_embd_gate; d++) {
+                    float v = data[e * n_embd_gate + d];
+                    sum_sq += (double) v * v;
+                }
+                norm_sum[e] += std::sqrt(sum_sq);
+            }
+            n_layers++;
+        }
+    }
+
+    // Average across layers
+    if (n_layers > 0) {
+        for (int e = 0; e < n_expert; e++) {
+            norm_sum[e] /= n_layers;
+        }
+    }
+
+    printf("  Computed gate norms across %d layers\n", n_layers);
+    return norm_sum;
+}
+
+// Snake draft: sort experts by score descending, then distribute to groups
+// in snake order (0,1,2,3,3,2,1,0,0,1,2,3,...) so each group gets balanced mix
+static std::vector<std::vector<int>> snake_draft(
+        const std::vector<double> & scores,
+        int n_groups) {
+    int n = (int)scores.size();
+
+    // Sort expert indices by score (descending)
+    std::vector<int> ranked(n);
+    std::iota(ranked.begin(), ranked.end(), 0);
+    std::sort(ranked.begin(), ranked.end(),
+        [&](int a, int b) { return scores[a] > scores[b]; });
+
+    std::vector<std::vector<int>> groups(n_groups);
+    for (int i = 0; i < n; i++) {
+        // Snake: round = i / n_groups, position in round = i % n_groups
+        int round = i / n_groups;
+        int pos   = i % n_groups;
+        int group = (round % 2 == 0) ? pos : (n_groups - 1 - pos);
+        groups[group].push_back(ranked[i]);
+    }
+
+    // Sort each group's expert IDs for cleaner output
+    for (auto & g : groups) {
+        std::sort(g.begin(), g.end());
+    }
+
+    // Print balance statistics
+    printf("\n  Snake draft balance:\n");
+    for (int g = 0; g < n_groups; g++) {
+        double sum = 0;
+        for (int e : groups[g]) sum += scores[e];
+        printf("    Group %d: %d experts, total norm = %.2f, mean norm = %.3f\n",
+            g, (int)groups[g].size(), sum, sum / groups[g].size());
+    }
+
+    return groups;
+}
+
 // Write a single group/expert-list GGUF
-// expert_ids: which original expert indices to include (renumbered 0..N-1 in output)
 static void write_group(
         const moe_split_params & params,
-        struct gguf_context * ctx_in,
-        struct ggml_context * ctx_meta,
-        std::ifstream & f_in,
+        const std::vector<input_shard> & input_shards,
         const std::vector<int> & expert_ids,
         const std::string & output_path,
         int n_expert,
-        int n_expert_tensors,
         const char * label) {
+    auto * ctx_in = input_shards[0].ctx_gguf;
     int experts_per_group = (int)expert_ids.size();
 
     printf("\n--- %s: %d experts → %s ---\n", label, experts_per_group, output_path.c_str());
@@ -176,7 +449,7 @@ static void write_group(
     if ((int)expert_ids.size() > 16) printf(" ... (%d more)", (int)expert_ids.size() - 16);
     printf("\n");
 
-    // Check if expert_ids are contiguous (optimization: can use single read)
+    // Check if expert_ids are contiguous
     bool contiguous = true;
     for (int i = 1; i < (int)expert_ids.size(); i++) {
         if (expert_ids[i] != expert_ids[i-1] + 1) {
@@ -185,42 +458,50 @@ static void write_group(
         }
     }
     if (contiguous) {
-        printf("  (contiguous range [%d, %d) — using fast path)\n",
+        printf("  (contiguous range [%d, %d) — fast path)\n",
             expert_ids[0], expert_ids[0] + experts_per_group);
+    } else {
+        printf("  (non-contiguous — gather path)\n");
     }
 
     // Create output GGUF context
     auto * ctx_out = gguf_init_empty();
-
-    // Copy all KV metadata
     gguf_set_kv(ctx_out, ctx_in);
 
-    int n_tensors = gguf_get_n_tensors(ctx_in);
-
-    // Find the architecture
+    // MoE split outputs are standalone derived GGUFs, not members of the
+    // original upstream split set.
+    gguf_remove_key(ctx_out, "split.no");
+    gguf_remove_key(ctx_out, "split.count");
+    gguf_remove_key(ctx_out, "split.tensors.count");
+    // Find architecture
     int arch_key = gguf_find_key(ctx_in, "general.architecture");
     std::string arch_name;
     if (arch_key >= 0) {
         arch_name = gguf_get_val_str(ctx_in, arch_key);
     }
 
-    // Update expert count metadata
+    // Update expert count
     std::string expert_count_key = arch_name + ".expert_count";
     gguf_set_val_u32(ctx_out, expert_count_key.c_str(), experts_per_group);
 
-    // Clamp expert_used_count if needed
+    // Clamp expert_used_count
     std::string eu_key_str = arch_name + ".expert_used_count";
-    int eu_idx_local = gguf_find_key(ctx_out, eu_key_str.c_str());
-    if (eu_idx_local >= 0) {
-        int eu_val = gguf_get_val_u32(ctx_out, eu_idx_local);
+    int eu_idx = gguf_find_key(ctx_out, eu_key_str.c_str());
+    if (eu_idx >= 0) {
+        int eu_val = gguf_get_val_u32(ctx_out, eu_idx);
         if (eu_val > experts_per_group) {
             printf("  Clamping expert_used_count: %d → %d\n", eu_val, experts_per_group);
             gguf_set_val_u32(ctx_out, eu_key_str.c_str(), experts_per_group);
         }
     }
 
-    // Create a separate ggml context for sliced tensors
-    size_t ctx_size = (size_t)(n_expert_tensors + 48 + 16) * ggml_tensor_overhead() + 4096;
+    // Create ggml context for ALL tensors (trunk + experts) across every input
+    // shard. The multi-shard path no longer has a single ctx_in tensor count.
+    int n_tensors_total = 0;
+    for (const auto & input_shard : input_shards) {
+        n_tensors_total += gguf_get_n_tensors(input_shard.ctx_gguf);
+    }
+    size_t ctx_size = (size_t)(n_tensors_total + 64) * ggml_tensor_overhead() + 4096;
     struct ggml_init_params ctx_params = {
         /*.mem_size   =*/ ctx_size,
         /*.mem_buffer =*/ nullptr,
@@ -230,78 +511,49 @@ static void write_group(
 
     size_t total_expert_bytes_saved = 0;
 
-    for (int i = 0; i < n_tensors; i++) {
-        const char * name = gguf_get_tensor_name(ctx_in, i);
-        struct ggml_tensor * t = ggml_get_tensor(ctx_meta, name);
-
-        if (is_expert_tensor(name)) {
-            int n_dims = ggml_n_dims(t);
-            int64_t ne[GGML_MAX_DIMS] = {1, 1, 1, 1};
-            for (int d = 0; d < n_dims; d++) {
-                ne[d] = t->ne[d];
-            }
+    for (const auto & input_shard : input_shards) {
+        int n_tensors = gguf_get_n_tensors(input_shard.ctx_gguf);
+        for (int i = 0; i < n_tensors; i++) {
+            const char * name = gguf_get_tensor_name(input_shard.ctx_gguf, i);
+            struct ggml_tensor * t = ggml_get_tensor(input_shard.ctx_meta, name);
+
+            if (is_expert_tensor(name) || is_router_gate(name)) {
+                int n_dims = ggml_n_dims(t);
+                int64_t ne[GGML_MAX_DIMS] = {1, 1, 1, 1};
+                for (int d = 0; d < n_dims; d++) ne[d] = t->ne[d];
+
+                int expert_dim = n_dims - 1;
+                if (ne[expert_dim] != n_expert) {
+                    fprintf(stderr, "warning: tensor %s has ne[%d]=%lld, expected %d. Copying as-is.\n",
+                        name, expert_dim, (long long)ne[expert_dim], n_expert);
+                    gguf_add_tensor(ctx_out, t);
+                    continue;
+                }
 
-            int expert_dim = n_dims - 1;
-            if (ne[expert_dim] != n_expert) {
-                fprintf(stderr, "warning: tensor %s has ne[%d]=%lld, expected %d experts. Copying as-is.\n",
-                    name, expert_dim, (long long)ne[expert_dim], n_expert);
+                int64_t ne_new[GGML_MAX_DIMS] = {1, 1, 1, 1};
+                for (int d = 0; d < n_dims; d++) ne_new[d] = ne[d];
+                ne_new[expert_dim] = experts_per_group;
+
+                struct ggml_tensor * t_new = ggml_new_tensor(ctx_new, t->type, n_dims, ne_new);
+                ggml_set_name(t_new, name);
+                gguf_add_tensor(ctx_out, t_new);
+
+                size_t bytes_per_expert = ggml_nbytes(t) / n_expert;
+                total_expert_bytes_saved += bytes_per_expert * (n_expert - experts_per_group);
+
+                if (is_router_gate(name)) {
+                    printf("  %-40s [%lld x %lld] → [%lld x %lld]  (router gate gathered)\n",
+                        name, (long long)ne[0], (long long)ne[1],
+                        (long long)ne_new[0], (long long)ne_new[1]);
+                } else {
+                    printf("  %-40s [%lld x %lld x %lld] → [%lld x %lld x %lld]\n",
+                        name,
+                        (long long)ne[0], (long long)ne[1], (long long)ne[2],
+                        (long long)ne_new[0], (long long)ne_new[1], (long long)ne_new[2]);
+                }
+            } else {
                 gguf_add_tensor(ctx_out, t);
-                continue;
             }
-
-            int64_t ne_new[GGML_MAX_DIMS] = {1, 1, 1, 1};
-            for (int d = 0; d < n_dims; d++) {
-                ne_new[d] = ne[d];
-            }
-            ne_new[expert_dim] = experts_per_group;
-
-            struct ggml_tensor * t_new = ggml_new_tensor(ctx_new, t->type, n_dims, ne_new);
-            ggml_set_name(t_new, name);
-            gguf_add_tensor(ctx_out, t_new);
-
-            size_t bytes_per_expert = ggml_nbytes(t) / n_expert;
-            total_expert_bytes_saved += bytes_per_expert * (n_expert - experts_per_group);
-
-            printf("  %-40s [%lld x %lld x %lld] → [%lld x %lld x %lld]  (%.1f MB → %.1f MB)\n",
-                name,
-                (long long)ne[0], (long long)ne[1], (long long)ne[2],
-                (long long)ne_new[0], (long long)ne_new[1], (long long)ne_new[2],
-                ggml_nbytes(t) / 1e6,
-                ggml_nbytes(t_new) / 1e6);
-        } else if (is_router_gate(name)) {
-            int n_dims = ggml_n_dims(t);
-            int64_t ne[GGML_MAX_DIMS] = {1, 1, 1, 1};
-            for (int d = 0; d < n_dims; d++) {
-                ne[d] = t->ne[d];
-            }
-
-            int expert_dim = n_dims - 1;
-            if (ne[expert_dim] != n_expert) {
-                fprintf(stderr, "warning: router gate %s has ne[%d]=%lld, expected %d. Copying as-is.\n",
-                    name, expert_dim, (long long)ne[expert_dim], n_expert);
-                gguf_add_tensor(ctx_out, t);
-                continue;
-            }
-
-            int64_t ne_new[GGML_MAX_DIMS] = {1, 1, 1, 1};
-            for (int d = 0; d < n_dims; d++) {
-                ne_new[d] = ne[d];
-            }
-            ne_new[expert_dim] = experts_per_group;
-
-            struct ggml_tensor * t_new = ggml_new_tensor(ctx_new, t->type, n_dims, ne_new);
-            ggml_set_name(t_new, name);
-            gguf_add_tensor(ctx_out, t_new);
-
-            size_t bytes_per_expert = ggml_nbytes(t) / n_expert;
-            total_expert_bytes_saved += bytes_per_expert * (n_expert - experts_per_group);
-
-            printf("  %-40s [%lld x %lld] → [%lld x %lld]  (router gate gathered)\n",
-                name,
-                (long long)ne[0], (long long)ne[1],
-                (long long)ne_new[0], (long long)ne_new[1]);
-        } else {
-            gguf_add_tensor(ctx_out, t);
         }
     }
 
@@ -314,7 +566,6 @@ static void write_group(
         return;
     }
 
-    // Write the output file
     printf("  Writing %s ...\n", output_path.c_str());
 
     std::ofstream f_out(output_path, std::ios::binary);
@@ -332,35 +583,48 @@ static void write_group(
     gguf_get_meta_data(ctx_out, meta_data.data());
     f_out.write((const char *)meta_data.data(), meta_size);
 
-    // Write tensor data — gather expert rows by expert_ids
+    // Write tensor data
     std::vector<uint8_t> read_buf;
     std::vector<uint8_t> gather_buf;
+    std::vector<std::ifstream> f_inputs;
+    f_inputs.reserve(input_shards.size());
+    for (const auto & input_shard : input_shards) {
+        f_inputs.emplace_back(input_shard.path, std::ios::binary);
+        if (!f_inputs.back().is_open()) {
+            fprintf(stderr, "error: cannot open %s\n", input_shard.path.c_str());
+            exit(1);
+        }
+    }
 
     for (int i = 0; i < gguf_get_n_tensors(ctx_out); i++) {
         const char * name = gguf_get_tensor_name(ctx_out, i);
 
-        int i_in = gguf_find_tensor(ctx_in, name);
-        if (i_in < 0) {
+        int shard_index = -1;
+        int i_in = -1;
+        const input_shard * source_shard =
+            find_input_shard_for_tensor(input_shards, name, &shard_index, &i_in);
+        if (!source_shard) {
             fprintf(stderr, "error: tensor %s not found in input\n", name);
             exit(1);
         }
 
-        struct ggml_tensor * t_in = ggml_get_tensor(ctx_meta, name);
-        size_t offset_in = gguf_get_data_offset(ctx_in) + gguf_get_tensor_offset(ctx_in, i_in);
+        struct ggml_tensor * t_in = ggml_get_tensor(source_shard->ctx_meta, name);
+        size_t offset_in =
+            gguf_get_data_offset(source_shard->ctx_gguf) +
+            gguf_get_tensor_offset(source_shard->ctx_gguf, i_in);
+        std::ifstream & f_in = f_inputs[shard_index];
 
         if (is_expert_tensor(name) || is_router_gate(name)) {
             size_t bytes_per_expert = ggml_nbytes(t_in) / n_expert;
             size_t total_out_bytes  = (size_t)experts_per_group * bytes_per_expert;
 
             if (contiguous) {
-                // Fast path: single contiguous read
                 size_t slice_offset = offset_in + (size_t)expert_ids[0] * bytes_per_expert;
                 read_buf.resize(total_out_bytes);
                 f_in.seekg(slice_offset);
                 f_in.read((char *)read_buf.data(), total_out_bytes);
                 f_out.write((const char *)read_buf.data(), total_out_bytes);
             } else {
-                // Gather path: read each expert's slice individually
                 gather_buf.resize(total_out_bytes);
                 read_buf.resize(bytes_per_expert);
                 for (int e = 0; e < experts_per_group; e++) {
@@ -374,7 +638,6 @@ static void write_group(
             }
             zeros(f_out, GGML_PAD(total_out_bytes, GGUF_DEFAULT_ALIGNMENT) - total_out_bytes);
         } else {
-            // Copy full tensor
             size_t n_bytes = ggml_nbytes(t_in);
             read_buf.resize(n_bytes);
             f_in.seekg(offset_in);
@@ -392,38 +655,57 @@ static void write_group(
     ggml_free(ctx_new);
 }
 
+// Helper: produce all groups via write_group
+static void produce_groups(
+        const moe_split_params & params,
+        const std::vector<input_shard> & input_shards,
+        const std::vector<std::vector<int>> & groups,
+        int n_expert) {
+    int n_groups = (int)groups.size();
+
+    if (params.group_id >= 0) {
+        if (params.group_id >= n_groups) {
+            fprintf(stderr, "error: group_id %d >= n_groups %d\n", params.group_id, n_groups);
+            exit(1);
+        }
+        char label[128];
+        snprintf(label, sizeof(label), "Group %d", params.group_id);
+        write_group(params, input_shards, groups[params.group_id],
+                    params.output, n_expert, label);
+    } else {
+        for (int g = 0; g < n_groups; g++) {
+            std::string path;
+            if (!params.output_prefix.empty()) {
+                char buf[1024];
+                snprintf(buf, sizeof(buf), "%s-%d.gguf", params.output_prefix.c_str(), g);
+                path = buf;
+            } else {
+                // Should not happen due to arg validation
+                fprintf(stderr, "error: no output prefix for multi-group mode\n");
+                exit(1);
+            }
+            char label[128];
+            snprintf(label, sizeof(label), "Group %d", g);
+            write_group(params, input_shards, groups[g],
+                        path, n_expert, label);
+        }
+    }
+}
+
 int main(int argc, const char ** argv) {
     moe_split_params params;
     parse_args(argc, argv, params);
 
-    // Load input GGUF metadata (no tensor data)
-    struct ggml_context * ctx_meta = nullptr;
-    struct gguf_init_params init_params = {
-        /*.no_alloc = */ true,
-        /*.ctx      = */ &ctx_meta,
-    };
-
-    auto * ctx_in = gguf_init_from_file(params.input.c_str(), init_params);
-    if (!ctx_in) {
-        fprintf(stderr, "error: failed to load %s\n", params.input.c_str());
-        return 1;
-    }
-
-    std::ifstream f_in(params.input, std::ios::binary);
-    if (!f_in.is_open()) {
-        fprintf(stderr, "error: cannot open %s\n", params.input.c_str());
-        return 1;
-    }
+    auto input_shards = load_input_shards(params.input);
+    auto * ctx_in = input_shards[0].ctx_gguf;
 
     // Read model metadata
-    int n_tensors = gguf_get_n_tensors(ctx_in);
     int arch_key = gguf_find_key(ctx_in, "general.architecture");
     std::string arch_name;
     if (arch_key >= 0) {
         arch_name = gguf_get_val_str(ctx_in, arch_key);
     }
 
-    // Find expert count
     std::string expert_key = arch_name + ".expert_count";
     int ec_idx = gguf_find_key(ctx_in, expert_key.c_str());
     if (ec_idx < 0) {
@@ -432,39 +714,47 @@ int main(int argc, const char ** argv) {
     }
     int n_expert = gguf_get_val_u32(ctx_in, ec_idx);
 
-    // Find expert_used count
     std::string eu_key = arch_name + ".expert_used_count";
     int eu_idx = gguf_find_key(ctx_in, eu_key.c_str());
-    int n_expert_used = eu_idx >= 0 ? gguf_get_val_u32(ctx_in, eu_idx) : 0;
+    int n_expert_used = eu_idx >= 0 ? (int)gguf_get_val_u32(ctx_in, eu_idx) : 0;
+
+    int n_tensors = 0;
+    for (const auto & input_shard : input_shards) {
+        n_tensors += gguf_get_n_tensors(input_shard.ctx_gguf);
+    }
 
     printf("Model: %s\n", params.input.c_str());
     printf("Architecture: %s\n", arch_name.c_str());
     printf("Experts: %d (top-%d)\n", n_expert, n_expert_used);
     printf("Tensors: %d\n", n_tensors);
 
-    // Count expert vs trunk tensors
+    // Count tensors
     int n_expert_tensors = 0;
     size_t expert_total_bytes = 0;
     size_t trunk_total_bytes = 0;
-    for (int i = 0; i < n_tensors; i++) {
-        const char * name = gguf_get_tensor_name(ctx_in, i);
-        struct ggml_tensor * t = ggml_get_tensor(ctx_meta, name);
-        if (is_expert_tensor(name)) {
-            n_expert_tensors++;
-            expert_total_bytes += ggml_nbytes(t);
-        } else {
-            trunk_total_bytes += ggml_nbytes(t);
+    for (const auto & input_shard : input_shards) {
+        int shard_tensors = gguf_get_n_tensors(input_shard.ctx_gguf);
+        for (int i = 0; i < shard_tensors; i++) {
+            const char * name = gguf_get_tensor_name(input_shard.ctx_gguf, i);
+            struct ggml_tensor * t = ggml_get_tensor(input_shard.ctx_meta, name);
+            if (is_expert_tensor(name)) {
+                n_expert_tensors++;
+                expert_total_bytes += ggml_nbytes(t);
+            } else {
+                trunk_total_bytes += ggml_nbytes(t);
+            }
         }
     }
 
-    if (!params.expert_list.empty()) {
-        // ======= Custom expert list mode =======
-        int experts_per_group = (int)params.expert_list.size();
+    printf("\nTrunk tensors: %d (%.1f MB)\n", n_tensors - n_expert_tensors, trunk_total_bytes / 1e6);
+    printf("Expert tensors: %d (%.1f MB total)\n", n_expert_tensors, expert_total_bytes / 1e6);
 
-        printf("Mode: custom expert list\n");
-        printf("Selected experts: %d\n", experts_per_group);
+    // ===== Mode dispatch =====
+
+    if (!params.expert_list.empty()) {
+        // --- Custom expert list ---
+        printf("Mode: custom expert list (%d experts)\n", (int)params.expert_list.size());
 
-        // Validate expert IDs
         for (int eid : params.expert_list) {
             if (eid < 0 || eid >= n_expert) {
                 fprintf(stderr, "error: expert ID %d out of range [0, %d)\n", eid, n_expert);
@@ -472,22 +762,117 @@ int main(int argc, const char ** argv) {
             }
         }
 
-        if (experts_per_group < n_expert_used) {
-            fprintf(stderr, "warning: selected %d experts < expert_used (%d). "
-                "Routing will be forced to reuse experts within group.\n",
-                experts_per_group, n_expert_used);
+        write_group(params, input_shards, params.expert_list,
+                    params.output, n_expert, "Custom");
+
+    } else if (!params.group_map_file.empty()) {
+        // --- Group map file ---
+        auto groups = read_group_map(params.group_map_file);
+        printf("Mode: group map from %s (%d groups)\n", params.group_map_file.c_str(), (int)groups.size());
+
+        if (params.n_groups > 0 && (int)groups.size() != params.n_groups) {
+            fprintf(stderr, "warning: group map has %d groups, --groups says %d. Using file.\n",
+                (int)groups.size(), params.n_groups);
+        }
+
+        // Validate
+        for (int g = 0; g < (int)groups.size(); g++) {
+            for (int eid : groups[g]) {
+                if (eid < 0 || eid >= n_expert) {
+                    fprintf(stderr, "error: group %d has expert ID %d out of range [0, %d)\n",
+                        g, eid, n_expert);
+                    return 1;
+                }
+            }
         }
 
-        printf("\nTrunk tensors: %d (%.1f MB)\n", n_tensors - n_expert_tensors, trunk_total_bytes / 1e6);
-        printf("Expert tensors: %d (%.1f MB total)\n", n_expert_tensors, expert_total_bytes / 1e6);
-        printf("Expected output size: %.1f MB\n",
-            (trunk_total_bytes + expert_total_bytes * experts_per_group / n_expert) / 1e6);
+        produce_groups(params, input_shards, groups, n_expert);
 
-        write_group(params, ctx_in, ctx_meta, f_in, params.expert_list,
-                    params.output, n_expert, n_expert_tensors, "Custom");
-    } else {
-        // ======= Contiguous group mode =======
-        printf("Mode: contiguous groups\n");
+    } else if (!params.ranking_file.empty()) {
+        // --- Ranking-based grouping from moe-analyze data ---
+        printf("Mode: ranking-based (snake draft by actual gate mass)\n");
+        printf("Groups: %d\n", params.n_groups);
+        printf("Ranking file: %s\n", params.ranking_file.c_str());
+        if (params.replicate > 0) {
+            printf("Replicating top %d hot experts to every group\n", params.replicate);
+        }
+
+        if (params.n_groups <= 0) {
+            fprintf(stderr, "error: --groups required with --ranking-file\n");
+            return 1;
+        }
+
+        auto ranking = read_ranking_file(params.ranking_file);
+        if ((int)ranking.size() != n_expert) {
+            fprintf(stderr, "warning: ranking has %d experts, model has %d\n",
+                (int)ranking.size(), n_expert);
+        }
+
+        // Identify experts to replicate (they go in EVERY group)
+        std::set<int> replicated;
+        for (int i = 0; i < params.replicate && i < (int)ranking.size(); i++) {
+            replicated.insert(ranking[i].first);
+            printf("  Replicating expert %d (mass=%.1f, %.1f%%)\n",
+                ranking[i].first, ranking[i].second,
+                ranking.size() > 0 ? 100.0 * ranking[i].second / ranking[0].second : 0);
+        }
+
+        // Remove replicated experts from the pool to draft
+        std::vector<std::pair<int,double>> pool;
+        for (auto & r : ranking) {
+            if (replicated.count(r.first) == 0) {
+                pool.push_back(r);
+            }
+        }
+
+        // Snake draft the remaining experts
+        int draft_per_group = (int)pool.size() / params.n_groups;
+        if (draft_per_group * params.n_groups != (int)pool.size()) {
+            fprintf(stderr, "warning: %d non-replicated experts not evenly divisible by %d groups\n",
+                (int)pool.size(), params.n_groups);
+        }
+
+        std::vector<std::vector<int>> groups(params.n_groups);
+
+        // Add replicated experts to every group first
+        for (int g = 0; g < params.n_groups; g++) {
+            for (int rep : replicated) {
+                groups[g].push_back(rep);
+            }
+        }
+
+        // Snake draft: pool is already sorted by mass desc
+        for (int i = 0; i < (int)pool.size(); i++) {
+            int round = i / params.n_groups;
+            int pos   = i % params.n_groups;
+            int group = (round % 2 == 0) ? pos : (params.n_groups - 1 - pos);
+            groups[group].push_back(pool[i].first);
+        }
+
+        // Sort and print
+        for (int g = 0; g < params.n_groups; g++) {
+            std::sort(groups[g].begin(), groups[g].end());
+            double sum = 0;
+            for (int e : groups[g]) {
+                for (auto & r : ranking) {
+                    if (r.first == e) { sum += r.second; break; }
+                }
+            }
+            printf("  Group %d: %d experts, total gate mass = %.1f\n",
+                g, (int)groups[g].size(), sum);
+        }
+
+        int epg = (int)groups[0].size();
+        printf("\nExperts per group: %d (%d drafted + %d replicated)\n",
+            epg, draft_per_group, params.replicate);
+        printf("Expected output size per node: %.1f MB\n",
+            (trunk_total_bytes + expert_total_bytes * epg / n_expert) / 1e6);
+
+        produce_groups(params, input_shards, groups, n_expert);
+
+    } else if (params.balanced) {
+        // --- Balanced grouping via router gate norms ---
+        printf("Mode: balanced (snake draft by router gate L2 norm)\n");
         printf("Groups: %d\n", params.n_groups);
 
         if (n_expert % params.n_groups != 0) {
@@ -496,55 +881,65 @@ int main(int argc, const char ** argv) {
             return 1;
         }
 
-        int experts_per_group = n_expert / params.n_groups;
-        printf("Experts per group: %d\n", experts_per_group);
+        // Compute gate norms
+        printf("\nAnalyzing router gate weights...\n");
+        auto norms = compute_gate_norms(input_shards, n_expert);
 
-        if (experts_per_group < n_expert_used) {
-            fprintf(stderr, "warning: experts_per_group (%d) < expert_used (%d). "
-                "Routing will be forced to reuse experts within group.\n",
-                experts_per_group, n_expert_used);
+        // Print top/bottom experts by norm
+        std::vector<int> ranked(n_expert);
+        std::iota(ranked.begin(), ranked.end(), 0);
+        std::sort(ranked.begin(), ranked.end(),
+            [&](int a, int b) { return norms[a] > norms[b]; });
+
+        printf("\n  Top 10 experts by avg gate L2 norm:\n");
+        for (int i = 0; i < std::min(10, n_expert); i++) {
+            printf("    Expert %3d: %.4f\n", ranked[i], norms[ranked[i]]);
+        }
+        printf("  Bottom 5 experts:\n");
+        for (int i = std::max(0, n_expert - 5); i < n_expert; i++) {
+            printf("    Expert %3d: %.4f\n", ranked[i], norms[ranked[i]]);
         }
 
-        printf("\nTrunk tensors: %d (%.1f MB)\n", n_tensors - n_expert_tensors, trunk_total_bytes / 1e6);
-        printf("Expert tensors: %d (%.1f MB total, %.1f MB per group)\n",
-            n_expert_tensors, expert_total_bytes / 1e6,
-            expert_total_bytes / (double)params.n_groups / 1e6);
+        // Snake draft
+        auto groups = snake_draft(norms, params.n_groups);
+
+        int epg = n_expert / params.n_groups;
+        printf("\nExperts per group: %d\n", epg);
         printf("Expected output size per node: %.1f MB\n",
             (trunk_total_bytes + expert_total_bytes / params.n_groups) / 1e6);
 
-        if (params.group_id >= 0) {
-            // Single group
-            if (params.group_id >= params.n_groups) {
-                fprintf(stderr, "error: group_id %d >= n_groups %d\n", params.group_id, params.n_groups);
-                return 1;
-            }
-            int start = params.group_id * experts_per_group;
-            std::vector<int> ids(experts_per_group);
-            for (int i = 0; i < experts_per_group; i++) ids[i] = start + i;
-
-            char label[64];
-            snprintf(label, sizeof(label), "Group %d [%d, %d)", params.group_id, start, start + experts_per_group);
-            write_group(params, ctx_in, ctx_meta, f_in, ids,
-                        params.output, n_expert, n_expert_tensors, label);
-        } else {
-            // All groups
-            for (int g = 0; g < params.n_groups; g++) {
-                int start = g * experts_per_group;
-                std::vector<int> ids(experts_per_group);
-                for (int i = 0; i < experts_per_group; i++) ids[i] = start + i;
-
-                char path[1024], label[64];
-                snprintf(path, sizeof(path), "%s-%d.gguf", params.output_prefix.c_str(), g);
-                snprintf(label, sizeof(label), "Group %d [%d, %d)", g, start, start + experts_per_group);
-                write_group(params, ctx_in, ctx_meta, f_in, ids,
-                            path, n_expert, n_expert_tensors, label);
-            }
+        produce_groups(params, input_shards, groups, n_expert);
+
+    } else {
+        // --- Contiguous grouping ---
+        printf("Mode: contiguous groups\n");
+        printf("Groups: %d\n", params.n_groups);
+
+        if (n_expert % params.n_groups != 0) {
+            fprintf(stderr, "error: n_expert (%d) must be divisible by n_groups (%d)\n",
+                n_expert, params.n_groups);
+            return 1;
         }
+
+        int epg = n_expert / params.n_groups;
+        printf("Experts per group: %d\n", epg);
+        printf("Expected output size per node: %.1f MB\n",
+            (trunk_total_bytes + expert_total_bytes / params.n_groups) / 1e6);
+
+        // Build contiguous groups
+        std::vector<std::vector<int>> groups(params.n_groups);
+        for (int g = 0; g < params.n_groups; g++) {
+            groups[g].resize(epg);
+            for (int i = 0; i < epg; i++) groups[g][i] = g * epg + i;
+        }
+
+        produce_groups(params, input_shards, groups, n_expert);
     }
 
-    f_in.close();
-    gguf_free(ctx_in);
-    ggml_free(ctx_meta);
+    for (const auto & input_shard : input_shards) {
+        gguf_free(input_shard.ctx_gguf);
+        ggml_free(input_shard.ctx_meta);
+    }
 
     printf("\nDone.\n");
     return 0;
-- 
2.50.1 (Apple Git-155)

